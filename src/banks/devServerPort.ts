/** Default TCP port for the scene banks REST API (`npm run dev:server`). Vite proxies `/api` here when BANKS_SERVER_URL is unset; override with BANKS_SERVER_URL / PORT env. */
export const BANKS_DEV_PORT = 9132;
